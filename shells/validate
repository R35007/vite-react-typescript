#!/bin/sh

concurrently \
  --kill-others-on-fail \
  --names "Lints,Build,Test" \
  --prefix-colors "yellow.bold,bgGreen.bold" \
  "npm:lint --silent" \
  "npm:build --silent" \
  "npm:test --silent"
